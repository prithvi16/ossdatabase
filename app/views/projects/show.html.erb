<% title @project.name %>
<% description "#{@project.name} : #{@project.tag_line}, ossdatabase"  %>
<div class="max-w-6xl mx-auto mt-8 px-4 sm:px-6 lg:px-8">
  <%= render(ProjectComponent.new(project: @project))%>
</div>
