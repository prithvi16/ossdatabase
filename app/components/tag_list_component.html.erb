<% @tag_list.each do |tag| %>
  <%= link_to(custom_tag_path(tag)) do %>
    <span class="inline-flex items-center text-white px-1.5 py-0.5 rounded text-xs uppercase <%= TAG_COLORS[tag.tag_type] %> ">
      <%= tag.name %>
    </span>
  <% end %>
<% end %>
